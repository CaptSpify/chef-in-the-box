# Dropped off by chef
domain <%= @domain %>
search <%= @domain %>

# I wanted to pull these dynamically, but I was learning and gave up.... or something
# dns1.example.com
nameserver 10.0.0.2
# dns2.example.com
nameserver 10.0.0.3
# dns3
nameserver 8.8.8.8
<% @nameserver.each do |ns| -%>
#nameserver <%= ns %>
<% end %>
options timeout 1
